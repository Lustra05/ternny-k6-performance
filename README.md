# ternny-k6-performance
A small k6 performance testing repo that runs weighted traffic across Ternny API endpoints, validates success/response-time thresholds, and generates a simple HTML report for each run. Includes configurable endpoints and environment-driven base URL support.
